# Transformer 与多头注意力：从零开始

> 目标：没有深度学习基础也能看懂。读完后再去看《多头注意力文献清单》就自然了。

---

## 第一章：什么是 RNN？——处理序列的"老前辈"

要看懂 Transformer，得先知道它取代了什么、为什么取代。Transformer 之前的霸主就是 RNN。

### 1.1 先说"序列"这个概念

很多数据是有顺序的：一句话的词有先后顺序，一段音频的帧有时间顺序，股票价格有日期先后。这种有序的数据叫**序列（Sequence）**。

处理序列的核心挑战是：**当前这个位置的信息，往往跟前面的内容有关。**

比如翻译"我喜欢猫" → "I like cats"，你在翻译"猫"的时候，必须知道前面有"我"和"喜欢"，才能正确翻译成"cats"而不是别的。换句话说，模型需要有某种"记忆"来记住之前看过的东西。

### 1.2 RNN 的核心想法：用一个"小本子"记住过去

RNN（Recurrent Neural Network，循环神经网络）的做法很直觉：

**给神经网络一个小本子（记忆向量），每读一个词，就把新信息和本子上的旧信息融合，更新本子，再处理下一个词。**

用一个生活类比：你在听课记笔记。
- 老师说了第一句话，你在本子上记下要点
- 老师说了第二句话，你把新信息和本子上的旧笔记融合，更新笔记
- 老师说了第三句话，再更新...
- 最后本子上的内容就是你对整堂课的"记忆"

RNN 做的就是这件事，只不过"本子"是一个固定长度的向量（比如 256 个数字），"融合"是一个矩阵乘法加激活函数。

### 1.3 RNN 具体怎么算的？（手算一个例子）

假设有一句话："猫 追 老鼠"，3 个词。RNN 的处理过程：

**准备工作**：
- 每个词先变成一个向量（词嵌入）：猫→[0.2, 0.5]，追→[0.8, -0.1]，老鼠→[-0.3, 0.4]
- 准备一个"记忆本子"h，初始全零：h₀ = [0, 0]
- 有两组可学习的权重：Wₕ（处理旧记忆）和 Wₓ（处理当前输入）

**第 1 步（读"猫"）**：

```
新记忆 h₁ = tanh( Wₕ · h₀ + Wₓ · 猫向量 )
           = tanh( Wₓ · [0.2, 0.5] )        # 因为 h₀ = [0,0]，第一项为 0
           = 比如得到 [0.3, 0.1]
```

现在"本子"上记的是：看到了"猫"。

**第 2 步（读"追"）**：

```
新记忆 h₂ = tanh( Wₕ · h₁ + Wₓ · 追向量 )
           = tanh( Wₕ · [0.3, 0.1] + Wₓ · [0.8, -0.1] )
           = 比如得到 [0.6, -0.2]
```

现在"本子"上记的是：看到了"猫"和"追"（两个词的信息融合在一起）。

**第 3 步（读"老鼠"）**：

```
新记忆 h₃ = tanh( Wₕ · h₂ + Wₓ · 老鼠向量 )
           = tanh( Wₕ · [0.6, -0.2] + Wₓ · [-0.3, 0.4] )
           = 比如得到 [0.4, 0.3]
```

最终 h₃ 就是 RNN 对整句话"猫追老鼠"的"记忆表示"，可以拿去做后续任务（比如分类、翻译等）。

**关键点**：每一步的计算方式完全一样，用的是同一组权重 Wₕ 和 Wₓ。这就是"循环"（Recurrent）的含义——同一个操作反复执行。

### 1.4 RNN 的数学表达

把上面的过程写成简洁的公式：

```
hₜ = tanh( Wₕ · hₜ₋₁ + Wₓ · xₜ )
```

- xₜ：第 t 个词的输入向量
- hₜ₋₁：上一步的记忆
- hₜ：当前步的记忆
- Wₕ, Wₓ：可学习的权重矩阵
- tanh：激活函数，把输出限制在 [-1, 1] 之间

就这么一个公式，反复用，就是 RNN 的全部核心。

### 1.5 RNN 的三个致命问题

RNN 的想法很直觉，但有三个严重的问题，最终导致它被 Transformer 取代。

**问题一：记不住太久远的东西（长距离依赖问题）**

回到记笔记的类比。假设老师讲了 50 句话，到第 50 句时你本子上的内容是 50 句话层层融合的结果。但第 1 句的信息经过 50 次"融合"后，已经被反复稀释覆盖，几乎看不出来了。

这在数学上叫**梯度消失**：反向传播时，梯度要从第 50 步传回第 1 步，中间要乘 50 次 Wₖ。如果每次乘的值小于 1，50 次连乘后梯度就接近 0，模型根本学不到远距离的关系。

打个比方：你玩过"传话游戏"吧？一句话经 10 个人传，最后已经面目全非。RNN 处理长序列就像一个超长的传话游戏。

**问题二：没法并行（训练慢）**

看 RNN 的计算流程：必须先算 h₁ 才能算 h₂，先算 h₂ 才能算 h₃……这是一个严格的串行过程。

GPU 的优势是并行计算（同时做几千个运算），但 RNN 没法利用这个优势——你得一步一步来。句子越长，训练越慢。这对大规模训练（比如现在的 LLM）是致命的。

**问题三：信息瓶颈（记忆容量有限）**

不管句子是 10 个词还是 10000 个词，RNN 的"记忆本子"始终是固定大小的向量（比如 256 维）。10000 个词的信息全部挤进 256 个数字里，必然丢失大量信息。

比喻：你拿一个 256 页的本子，想把整本《红楼梦》的内容都记上去，记到最后前面写的早就被覆盖了。

### 1.6 后续改进：LSTM 和 GRU

为了缓解 RNN 的记忆问题，后来提出了改进版本：

**LSTM（Long Short-Term Memory，长短期记忆网络，1997 年）** 在记忆本子旁边加了三个"门"：
- **遗忘门**：决定旧记忆中哪些该忘掉
- **输入门**：决定新输入中哪些该写入记忆
- **输出门**：决定记忆中哪些该输出

类比：你记笔记时有了策略——主动判断哪些内容该擦掉（遗忘门）、哪些新内容该记下来（输入门）、哪些该当成当前重点（输出门）。

**GRU（Gated Recurrent Unit，2014 年）** 是 LSTM 的简化版，把三个门合并成两个，计算更快，效果接近。

但 LSTM 和 GRU 只是缓解了问题一（长距离依赖），问题二（没法并行）和问题三（固定记忆瓶颈）依然存在。这三个问题的根本原因在于 RNN 的串行结构本身——**只要你还是"一步一步传递信息"，就不可避免地慢、不可避免地遗忘。**

这就引出了下一步的关键问题：**能不能不一步步传，让每个位置直接看到所有位置？**

→ 这就是注意力机制的出发点，下一章详细讲。

---

## 第二章：为什么要搞出"注意力"这个东西？

### 2.1 先说一个直觉

假设你在读这句话：

> "小明把苹果给了小红，因为**她**已经吃完了。"

这里的"她"指的是谁？你当然知道是小红。但你想过你是怎么判断的吗？

你的大脑在做一件事：在读到"她"这个字的时候，你会自动去回看前面所有的词，然后判断"她"跟"小红"的关系最紧密。这个"回头看，分配关注度"的过程，就是**注意力（Attention）**。

### 2.2 RNN 的困境回顾

上一章讲了 RNN 的三个问题：记不住远处的词、没法并行、记忆瓶颈。注意力机制正是为了彻底跳出这三个困境。

### 2.3 注意力机制：跳出"逐字阅读"的框框

2014-2015 年，研究者提出：**与其让信息逐层传递，不如让每个词直接"看到"句子中所有其他词，自己判断该关注谁。**

这就是注意力机制的核心思想：**不再串行地传递信息，而是让每个位置直接和所有位置建立联系。**

这个改动看似简单，但解决了 RNN 的三个问题：
- 远距离信息可以直接获取（不需要中间传递，"她"直接看到"小红"）
- 所有位置可以并行计算（所有词的注意力可以同时算）
- 不需要把所有信息压缩到一个固定向量中（每个词都有自己的表示）

---

## 第三章：注意力机制到底怎么算的？（Q、K、V）

### 3.1 一个搜索引擎的类比

理解注意力机制，关键是理解三个字母：**Q（Query）、K（Key）、V（Value）**。

用搜索引擎来类比：

| 概念 | 搜索引擎类比 | 在注意力中的作用 |
|------|-------------|----------------|
| **Q（Query，查询）** | 你输入的搜索关键词 | 当前这个词"想找"什么样的信息 |
| **K（Key，键）** | 每个网页的标题/标签 | 每个词"可以提供"什么样的信息 |
| **V（Value，值）** | 每个网页的实际内容 | 每个词的实际信息内容 |

当你搜索"深度学习"时：
1. 你的 Query（"深度学习"）和每个网页的 Key（标题）做比对 → 得到相关度分数
2. 按分数对所有网页的 Value（内容）做加权求和 → 得到搜索结果

注意力机制做的完全是一样的事情，只不过是在词与词之间。

### 3.2 具体计算过程（手算一个例子）

假设有一句话："猫 追 老鼠"，3 个词。

**第一步：每个词生成自己的 Q、K、V**

每个词通过三个不同的线性变换（权重矩阵 Wq、Wk、Wv），生成自己的 Q、K、V 向量：

```
猫 → Q猫, K猫, V猫
追 → Q追, K追, V追
老鼠 → Q鼠, K鼠, V鼠
```

**第二步：计算注意力分数**

要算"猫"这个词的新表示，就用 Q猫 去和所有词的 K 做点积（衡量相似度）：

```
score(猫, 猫) = Q猫 · K猫   → 比如得到 3.2
score(猫, 追) = Q猫 · K追   → 比如得到 1.1
score(猫, 鼠) = Q猫 · K鼠   → 比如得到 0.4
```

**第三步：归一化（Softmax）**

把分数转成概率（加起来等于 1）：

```
softmax([3.2, 1.1, 0.4]) → [0.80, 0.15, 0.05]
```

这意味着"猫"这个词 80% 关注自己，15% 关注"追"，5% 关注"老鼠"。

**第四步：加权求和**

用这些权重对所有词的 V 加权求和，得到"猫"的新表示：

```
新猫 = 0.80 × V猫 + 0.15 × V追 + 0.05 × V鼠
```

对"追"和"老鼠"也做同样的操作，每个词都得到一个新的、融合了上下文信息的表示。

### 3.3 一句话总结

注意力机制 = **用 Q 找到相关的 K，用相关度对 V 加权求和，得到融合了上下文的新表示**。

用公式写就是：

```
Attention(Q, K, V) = softmax(Q · K^T / √dk) · V
```

其中 √dk 是缩放因子，防止点积值太大导致 softmax 梯度消失。dk 是 K 向量的维度。

### 3.4 容易搞混的两个"维度"：dk 和 N

注意力计算里出现了两种不同的"长度"，初学很容易搞混，这里专门区分一下。

**dk（向量维度）**：每个词的 Q、K、V 向量有多少个数字。描述的是"一个词的信息有多丰富"。比如 dk=3，就是用 3 个数字来描述一个词。

**N（词的个数）**：句子里有多少个词。决定了注意力权重有几个值。

用"猫追老鼠"的完整数字例子来看清楚两者的区别（N=3 个词，dk=3 维）：

```
Q猫 = [0.2, 0.8, -0.1]     ← dk=3，每个向量有 3 个数
K猫 = [0.5, 0.3, 0.7]
K追 = [0.1, 0.9, -0.2]
K鼠 = [-0.4, 0.2, 0.6]
```

**点积是"横着"算的：把 dk 个数压成 1 个分数**

```
Q猫·K猫 = 0.2×0.5 + 0.8×0.3 + (-0.1)×0.7 = 0.27  → 1个分数
Q猫·K追 = 0.2×0.1 + 0.8×0.9 + (-0.1)×(-0.2) = 0.76  → 1个分数
Q猫·K鼠 = 0.2×(-0.4) + 0.8×0.2 + (-0.1)×0.6 = 0.02  → 1个分数
```

每次点积把 dk=3 个数压缩成 1 个数。做了 N=3 次点积，得到 N=3 个分数。

**softmax 是"竖着"算的：在 N 个分数之间做归一化**

```
softmax([0.27, 0.76, 0.02]) → [0.30, 0.49, 0.21]
         猫     追    老鼠       猫     追    老鼠
```

得到 N=3 个注意力权重，加起来等于 1。

**用权重对 V 加权求和，得到最终的新表示**

```
V猫 = [0.1, 0.6]      ← V 的维度可以和 dk 不同
V追 = [0.9, -0.3]
V鼠 = [0.4, 0.5]

猫的新表示 = 0.30 × [0.1, 0.6] + 0.49 × [0.9, -0.3] + 0.21 × [0.4, 0.5]
           = [0.03, 0.18]     + [0.44, -0.15]       + [0.08, 0.11]
           = [0.55, 0.14]
```

整个流程画成图：

```
dk 个数                     1 个数
Q猫·K猫 ──点积──→  0.27 ─┐
Q猫·K追 ──点积──→  0.76 ─┼── softmax ──→ [0.30, 0.49, 0.21] ──加权V──→ [0.55, 0.14]
Q猫·K鼠 ──点积──→  0.02 ─┘                ↑                              ↑
                                     N=3 个权重                     猫的新表示
```

总结：**dk 决定了点积时"有多少项相乘相加"，N 决定了"要和多少个词算点积"。注意力权重的长度等于 N（词的个数），和 dk 无关。**

---

## 第四章：自注意力（Self-Attention）

### 4.1 什么是"自"

上面的例子里，Q、K、V 全部来自同一个句子的不同变换，这就叫**自注意力（Self-Attention）**。

"自"的意思是：句子内部各个词之间互相注意，不需要外部的信息。

### 4.2 为什么自注意力很厉害？

回到"她"的例子：

> "小明把苹果给了小红，因为她已经吃完了。"

当模型处理"她"这个词时，自注意力让"她"直接和句子中所有词计算注意力分数。"她"的 Q 会和小红的 K 产生高分数（因为它们的语义相关），最终"她"的表示会融合大量来自"小红"的 V。

这就是为什么 Transformer 能很好地处理指代消解、长距离依赖等问题——**每个词都能直接访问任何位置的词，距离不再是障碍。**

和 RNN 对比：RNN 中"她"要等信息从"小红"一路传过来，可能已经模糊了；自注意力中"她"直接和"小红"算分数，一步到位。

---

## 第五章：Transformer 的整体架构

### 5.1 一张图说清楚

Transformer（原始 2017 版）是一个 Encoder-Decoder 结构，主要用于机器翻译：

```
输入句子（如"Hello world"）
    │
    ▼
┌─────────────────────────────┐
│  Encoder（编码器）× 6 层      │
│                             │
│  ① 词嵌入 + 位置编码          │
│  ② 自注意力层                │  ← 每个词看到所有词
│  ③ 前馈神经网络层（FFN）      │  ← 逐位置非线性变换
│  ④ 残差连接 + LayerNorm      │
│                             │
└─────────────────────────────┘
    │
    │  编码后的表示（每个词一个向量）
    ▼
┌─────────────────────────────┐
│  Decoder（解码器）× 6 层      │
│                             │
│  ① 词嵌入 + 位置编码          │
│  ② 掩码自注意力层            │  ← 只能看到已生成的内容
│  ③ 交叉注意力层              │  ← 关注 Encoder 的输出
│  ④ 前馈神经网络层（FFN）      │
│  ⑤ 残差连接 + LayerNorm      │
│                             │
└─────────────────────────────┘
    │
    ▼
  输出句子（如"你好 世界"）
```

### 5.2 逐个组件解释

**① 词嵌入（Embedding）**：把每个词变成一个向量。比如"猫"可能变成 [0.2, -0.5, 0.8, ...]。这些向量是可学习的，训练过程中不断调整。

**② 位置编码（Positional Encoding）**：自注意力有个问题——它不区分顺序。"猫追老鼠"和"老鼠追猫"在自注意力看来是一样的（因为每个词都能看到所有词，但不知道谁在前谁在后）。所以需要额外加上位置信息，告诉模型"这是第 1 个词""这是第 2 个词"。

最简单的想法是直接用数字编号：第 1 个词标 1，第 2 个词标 2，第 100 个词标 100。但问题是数字大小差异太大，第 1 个词和第 100 个词的编号差了 100 倍，会干扰模型训练。

正弦余弦的巧妙之处在于，它像一个**时钟**。想象三个指针：秒针转得快、分针转得慢、时针转得更慢。你看一眼时钟，三根针的组合就唯一确定了一个时刻。不管是 1 点还是 12 点，所有指针的值都在固定范围内转，没有"越来越大"的问题。

位置编码做的就是类似的事情。对每个位置，用一组不同频率的正弦波来编码。举个简化的例子，假设位置编码只有 2 维：

```
位置0：PE = [sin(0), cos(0)]   = [0.00, 1.00]
位置1：PE = [sin(1), cos(1)]   = [0.84, 0.54]
位置2：PE = [sin(2), cos(2)]   = [0.91, -0.42]
位置3：PE = [sin(3), cos(3)]   = [0.14, -0.99]
位置4：PE = [sin(4), cos(4)]   = [-0.76, -0.65]
```

每个位置得到一组独特的数字，所有值都在 -1 到 1 之间，不会越来越大。而且不同频率的正弦波叠加在一起，每个位置的组合都不同，模型能区分开。

实际模型中维度是 512，就像有 256 根指针（每根指针贡献一个 sin 和一个 cos），频率从快到慢覆盖各种尺度，既能区分相邻的词，也能区分距离很远的词。

然后把位置编码直接**加到**词嵌入上：

```
"猫"的最终输入 = 词嵌入([0.2, 0.5]) + 位置编码([0.00, 1.00]) = [0.20, 1.50]
"追"的最终输入 = 词嵌入([0.8, -0.1]) + 位置编码([0.84, 0.54]) = [1.64, 0.44]
```

这样每个词既带着"我是什么词"的信息，也带着"我在第几个位置"的信息。

原始论文的公式是：
```
PE(pos, 2i) = sin(pos / 10000^(2i/d))
PE(pos, 2i+1) = cos(pos / 10000^(2i/d))
```

**③ 前馈网络（FFN）**：自注意力做的事情是让词与词之间交换信息——"猫"从"追"和"老鼠"那里借信息，更新自己的表示。但自注意力本质上只是做了加权求和，这是一个**线性操作**（乘以权重再加起来）。线性操作的表达能力很弱，就像只能画直线，但很多关系是弯曲的、复杂的。

FFN 的作用就是给每个词的表示加一层**非线性变换**，让模型能学到更复杂的特征。用生活类比：自注意力像开会讨论，大家交换观点；FFN 像会后每个人独立思考消化，把听到的信息整理成自己的理解。

具体怎么做的？拿"猫追老鼠"举例，经过自注意力后，三个词各自得到了一个新表示：

```
自注意力输出：
猫 → [0.55, 0.14]    ← 已经融合了上下文信息
追 → [0.32, 0.71]
老鼠 → [0.18, 0.43]
```

FFN 对**每个词的向量独立**做两层变换。先看"猫"：

```
第一层：先乘以权重矩阵 W1，再加偏置 b1
[0.55, 0.14] × W1 + b1 → 比如得到 [0.9, -0.3, 0.7]  （维度可以变，这里是 3 维）

激活函数：把负数变成 0
max(0, [0.9, -0.3, 0.7]) → [0.9, 0, 0.7]

第二层：再乘以权重矩阵 W2，再加偏置 b2
[0.9, 0, 0.7] × W2 + b2 → 比如得到 [0.42, 0.88]  （变回原来的维度）
```

"追"和"老鼠"也各自独立走一遍同样的流程：

```
追 → [0.32, 0.71] → 第一层 → 激活 → 第二层 → [0.61, 0.05]
老鼠 → [0.18, 0.43] → 第一层 → 激活 → 第二层 → [0.27, 0.55]
```

关键点：三个词用的是**同一组 W1、b1、W2、b2**（同一套参数），但因为输入不同，输出也不同。而且每个词只对自己的向量做变换，词与词之间不再交换信息——这就是"对每个词独立做"的意思。

用公式写就是：
```
FFN(x) = max(0, x·W1 + b1) · W2 + b2
```

总结：**自注意力负责"词与词之间的信息交互"，FFN 负责"每个词自己的深度加工"。两者交替进行，一层层往上抽象。**

**④ 残差连接 + LayerNorm**：这两个和注意力里的点积、softmax 完全不是一回事，只是"归一化"这个词恰好都出现了。

**残差连接**特别简单：就是把输入原封不动地加到输出上。

还记得 3.4 节"猫追老鼠"的例子吗？"猫"经过自注意力后算出了新表示 [0.55, 0.14]。但"猫"在进入自注意力之前也有一个原始输入向量（词嵌入），假设是 [0.2, 0.5]。残差连接就是把这俩加起来：

```
"猫"的输入 x（词嵌入）         = [0.2, 0.5]
"猫"经过自注意力后的 output    = [0.55, 0.14]  ← 就是 3.4 节算出来的那个
残差连接的结果 = output + x    = [0.55+0.2, 0.14+0.5] = [0.75, 0.64]
```

完整回顾一下这个流程：3.4 节里"猫"拿着自己的 Q 去和所有词的 K 算点积、经过 softmax 得到注意力权重 [0.30, 0.49, 0.21]、再对 V 加权求和得到 [0.55, 0.14]——这就是自注意力的 output。然后残差连接把这个 output 和原始输入 x 加起来，得到 [0.75, 0.64]。这个 [0.75, 0.64] 才是这一层最终传给下一层的结果。

```
词嵌入 [0.2, 0.5]
   │
   ├──→ 自注意力（Q·K → softmax → 加权V）──→ [0.55, 0.14]
   │                                          │
   └──────────────── 加起来（残差连接）─────────┘
                                              │
                                              ▼
                                    [0.75, 0.64] → 传给下一层
```

为什么要这么做？Transformer 有 6 层甚至更多层，层数一多，信息在层层变换中可能丢失或走偏。残差连接相当于给每一层留了一条**捷径**——就算这一层什么都没学好，至少原始信息还在，不会越变越差。

**LayerNorm** 是把向量里的数字标准化到统一的范围。

```
输入 [0.6, 0.1]
计算平均值 = (0.6+0.1)/2 = 0.35
计算标准差 = 0.25（衡量数字离平均值多远）
归一化 = [(0.6-0.35)/0.25, (0.1-0.35)/0.25] = [1.0, -1.0]
```

为什么需要它？训练神经网络时，每层的输出数值范围可能差异很大，有的层输出值在 0.001 附近，有的层在 1000 附近。这会让训练很不稳定。LayerNorm 就是每一层都把数值范围拉回来，保证训练稳定。

简单对比一下两者的区别：

```
注意力的点积 + softmax：目的是算词与词之间的相关度
残差连接 + LayerNorm：  目的是让深层网络训练更稳定
```

它们在不同的地方做不同的事，不要搞混。

**⑤ 掩码自注意力（Masked Self-Attention）**：

注意：前面②的自注意力在 Encoder 里用，每个词看到所有词，不需要掩码。而掩码自注意力是 **Decoder 专属**的。

为什么需要掩码？因为 Decoder 是"逐步生成"输出的。比如翻译 "I love cats" → "我 爱 猫"：

```
第1步：看到 Encoder 对英文的理解 → 生成"我"
第2步：看到"我" + Encoder 的理解 → 生成"爱"     ← 此时"猫"还没生成
第3步：看到"我 爱" + Encoder 的理解 → 生成"猫"
```

生成"爱"的时候"猫"还不存在，所以不能看到它。掩码就是把未来的位置遮住——在注意力分数矩阵的上三角部分填 -∞，softmax 后这些位置变为 0。

```
Encoder 的自注意力：   输入是完整的 → 每个词看到所有词 → 不需要掩码
Decoder 的掩码自注意力：输出是逐步生成的 → 只能看到已有的词 → 需要掩码
```

**⑥ 交叉注意力（Cross-Attention）**：

Decoder 不仅要关注自己已生成的词（通过⑤掩码自注意力），还要"回头看"Encoder 的理解结果。这时 Q 来自 Decoder（"我想知道下一个词该翻译什么"），K 和 V 来自 Encoder（"英文原句的完整理解"）。

用翻译 "I love cats" → "我 爱 猫" 的完整例子来看 Encoder 和 Decoder 是怎么配合的：

```
用户输入 "I love cats"
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Encoder（编码器）                                 │
│                                                  │
│  "I love cats" 三个词各自做自注意力，互相交换信息    │
│  每个词都能看到所有词（不需要掩码）                  │
│                                                  │
│  最终输出三个向量：                                 │
│    I    → [0.8, -0.2, 0.5, ...]                  │
│    love → [0.3, 0.7, -0.1, ...]                  │
│    cats → [-0.4, 0.6, 0.9, ...]                  │
│                                                  │
│  这三个向量就是 Encoder "对英文的理解"               │
│  它们作为 K 和 V，一直放在那里供 Decoder 随时查看    │
└──────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Decoder（解码器）                                 │
│                                                  │
│  第1步：手里只有 <start>                           │
│    ① 掩码自注意力：<start> 只看自己                 │
│    ② 交叉注意力：拿 <start> 的 Q，去 Encoder       │
│       那三个向量（I, love, cats 的 K 和 V）里查     │
│       → 发现最该关注 "I"                           │
│    ③ FFN 加工 → 从5万个词里选概率最高的              │
│    → 输出"我"                                     │
│                                                  │
│  第2步：手里有 [<start>, 我]                       │
│    ① 掩码自注意力："我"只看 <start> 和自己           │
│    ② 交叉注意力：拿"我"的 Q，去 Encoder             │
│       那三个向量里查                                │
│       → 发现最该关注 "love"                        │
│    ③ FFN 加工 → 选概率最高的                        │
│    → 输出"爱"                                     │
│                                                  │
│  第3步：手里有 [<start>, 我, 爱]                    │
│    ① 掩码自注意力："爱"只看前面的词                   │
│    ② 交叉注意力：去 Encoder 那三个向量里查            │
│       → 发现最该关注 "cats"                        │
│    ③ FFN 加工 → 选概率最高的                        │
│    → 输出"猫"                                     │
│                                                  │
│  第4步 → 输出 <end>，结束                          │
└──────────────────────────────────────────────────┘
```

用搜索引擎的类比来理解：Encoder 的输出就像三个网页已经建好了索引，放在搜索引擎的数据库里（K 和 V）。Decoder 每一步都在搜索引擎里输入一个新的关键词（Q），查一遍该看哪个网页，拿到信息后决定下一个词输出什么。

**补充说明：不是所有任务都需要 Encoder + Decoder**

原始 Transformer 用 Encoder + Decoder 是为了翻译这类"输入一个序列、输出另一个序列"的任务。但后来的模型根据需要只用其中一部分：只需"理解"不需"生成"（如文本分类）→ 只用 Encoder（如 BERT）；只需"生成"（如聊天对话）→ 只用 Decoder（如 GPT）。

### 5.3 为什么 Transformer 碾压了 RNN？

| 特性 | RNN | Transformer |
|------|-----|-------------|
| 长距离依赖 | 差（信息要逐层传递，会遗忘） | 好（任意两词直接计算注意力） |
| 并行计算 | 不行（必须串行） | 可以（所有位置同时计算） |
| 计算复杂度 | O(N) 序列长度 | O(N²) 序列长度（这是新问题） |
| 可解释性 | 差 | 好（可以看注意力分数矩阵） |

Transformer 的缺点是 O(N²) 复杂度——序列越长，计算量增长越快。这也是后来 Longformer、FlashAttention 等工作要解决的问题。

---

## 第六章：多头注意力（Multi-Head Attention）

这一章是重点——你老师让你讲的就是这个。

### 6.1 单头注意力的问题

到目前为止我们说的注意力只有一个"头"——即一组 Q、K、V 变换。

问题是：一个头只能学到一种注意力模式。

比如处理 "The animal didn't cross the street because it was too tired" 这句话：
- "it" 可能需要关注 "animal"（语法上，主语指代）
- "it" 也可能需要关注 "tired"（语义上，状态描述）

如果只有一个头，它只能学到一种模式，可能顾此失彼。

### 6.2 多头注意力的想法

**思路很简单：与其只用一个头，不如用多个头，每个头学到不同的注意力模式，最后把结果拼起来。**

原始论文用 8 个头。具体做法：

```
第一步：用 8 组不同的变换矩阵，把 Q、K、V 分别投影到 8 个不同的子空间

  Head 1: Q₁ = X·Wq₁,  K₁ = X·Wk₁,  V₁ = X·Wv₁
  Head 2: Q₂ = X·Wq₂,  K₂ = X·Wk₂,  V₂ = X·Wv₂
  ...
  Head 8: Q₈ = X·Wq₈,  K₈ = X·Wk₈,  V₈ = X·Wv₈

第二步：每个头独立计算注意力

  Head₁ = Attention(Q₁, K₁, V₁)
  Head₂ = Attention(Q₂, K₂, V₂)
  ...
  Head₈ = Attention(Q₈, K₈, V₈)

第三步：把 8 个头的输出拼接起来，再通过一个线性变换

  MultiHead = Concat(Head₁, ..., Head₈) · Wo
```

### 6.3 用比喻理解

想象你在读一篇文章：
- **头 1** 专门关注语法关系（主谓宾）
- **头 2** 专门关注指代关系（"他"指谁）
- **头 3** 专门关注标点符号附近的句子边界
- **头 4** 专门关注否定词和修饰对象
- ...

每个头从不同角度看同一句话，最后综合所有视角得到全面的表示。这就是多头注意力的核心直觉。

### 6.4 关键细节：维度分配

如果原始词向量维度是 512，用 8 个头，那么每个头的维度是 512/8 = 64。

**重要澄清：每个头看到的输入是完整的 512 维，不是只看 1/8。**

容易搞混的地方在这里。512/8=64 算的是每个头的**输出维度**，不是每个头能看到的输入范围。每个头都看完整的 512 维输入，通过各自的投影矩阵压缩成 64 维输出。

对比一下错误理解和正确做法：

**错误理解（切片）：把 512 维切成 8 份，每个头拿一块**

```
原始向量：[v1, ..., v64 | v65, ..., v128 | ... | v449, ..., v512]

Head 1 只拿 [v1, ..., v64]          ← 只看到 1/8 的信息 ✗
Head 2 只拿 [v65, ..., v128]        ← 只看到另外 1/8 ✗
```

如果这样做，每个头确实丢失了信息。

**实际做法（投影）：每个头都看完整的 512 维，压缩成 64 维**

```
原始向量 x = [v1, v2, ..., v512]  （512个数，每个头都能全部看到）

Head 1：Q1 = x × Wq1    （Wq1 是 512×64 的矩阵）→ 输出 64 个数
Head 2：Q2 = x × Wq2    （Wq2 是 512×64 的矩阵）→ 输出 64 个数
...
Head 8：Q8 = x × Wq8    （Wq8 是 512×64 的矩阵）→ 输出 64 个数
```

每个头的投影矩阵是 512×64 的，意味着 512 个输入全部参与计算，通过加权求和压缩成 64 个输出。就像 8 个人看同一张完整的大照片，但每个人戴着不同颜色的滤镜——大家看到的是同一张全量照片，只是各自关注的方面不同。

用具体数字感受一下：

```
x = [0.3, -0.1, 0.8, 0.2, ..., 0.5]   （512个数）

Head 1 的 Q1 第一个输出值 = 0.3×w1 + (-0.1)×w2 + 0.8×w3 + ... + 0.5×w512
                           ↑ 全部 512 个值都参与了，加权求和压缩成 1 个数

Head 2 的 Q2 第一个输出值 = 0.3×w1' + (-0.1)×w2' + 0.8×w3' + ... + 0.5×w512'
                           ↑ 同样全部 512 个值参与，但权重不同，所以结果不同
```

每个头输出 64 个数，8 个头就是 8×64=512 个数，拼起来维度和原来一样。最后再乘一个矩阵 Wo 把 8 个头的结果混合，得到最终输出。

这样做总计算量和单头差不多（因为每个头更小了），但获得了 8 个不同的注意力模式。原始论文的表述是：

> "Multi-head attention allows the model to jointly attend to information from different representation subspaces at different positions."

翻译：多头注意力让模型能够同时从不同的表示子空间、在不同位置上关注信息。

### 6.5 多头注意力在 Transformer 中的位置

Transformer 的三个地方用了多头注意力：

```
Encoder 中的自注意力：
  Q, K, V 都来自编码器输入
  每个词看到输入句子的所有词

Decoder 中的掩码自注意力：
  Q, K, V 都来自解码器输入（已生成的部分）
  每个词只看到自己和之前的词（未来被屏蔽）

Decoder 中的交叉注意力：
  Q 来自解码器
  K, V 来自编码器输出
  解码器关注源语言句子的表示
```

---

## 第七章：多头注意力后来发生了什么？（衔接文献清单）

现在你理解了多头注意力的基本原理，可以回看那份文献清单了。这里先给你一个简明的衔接。

### 7.1 多头真的有用吗？有冗余吗？

研究发现（文献 2-5）：很多头其实是冗余的，剪掉大部分头性能几乎不变。但少数头确实学到了有用的功能（句法、指代等）。

→ 这就引出了"如何让头学到真正不同的东西"的研究（文献 6-7）。

### 7.2 头太多了，推理太慢怎么办？

大模型中，多头注意力需要为每个头存储独立的 K、V 向量。当模型有几十上百层、每层几十个头时，这些 K/V 缓存（KV Cache）占用巨大内存。

→ 这就引出了 MQA（共享 K/V）→ GQA（分组共享）→ MLA（压缩恢复）的演进路线（文献 8-10）。

### 7.3 能不能让头更智能地组合？

标准 MHA 中所有头独立计算、简单拼接。研究者觉得这太死板了。

→ DCMHA 让头之间动态交互，MoH 借鉴 MoE 思想只激活部分头（文献 14-15）。

### 7.4 序列太长，O(N²) 太贵怎么办？

→ 稀疏注意力（Longformer, BigBird）只计算部分词对之间的注意力，FlashAttention 优化底层实现（文献 11-13, 19）。

---

## 附录：关键术语速查

| 术语 | 含义 |
|------|------|
| RNN（循环神经网络） | 逐步处理序列，用"记忆向量"传递信息 |
| LSTM（长短期记忆网络） | RNN 的改进版，用"门"控制记忆的读写 |
| GRU | LSTM 的简化版，效果接近但更快 |
| Attention（注意力） | 用 Q 和 K 的相似度对 V 加权求和 |
| Self-Attention（自注意力） | Q、K、V 来自同一个序列 |
| Multi-Head Attention（多头注意力） | 并行做多次注意力，每个头在不同子空间 |
| Q（Query） | 当前词"想找"的信息 |
| K（Key） | 每个词"可提供"的信息标签 |
| V（Value） | 每个词的实际信息内容 |
| KV Cache | 推理时缓存的 Key/Value 张量，避免重复计算 |
| FFN（前馈网络） | 对每个位置独立做的非线性变换 |
| Positional Encoding | 给注意力补充位置信息 |
| Residual Connection | 跳跃连接，防止梯度消失 |
| LayerNorm | 层归一化，稳定训练 |
| Softmax | 把任意实数转成概率分布（加起来=1） |
| tanh | 激活函数，把输出限制在 [-1, 1] |
| 梯度消失 | 反向传播时梯度越乘越小，导致深层学不动 |
| dk | Key 向量的维度 |
| Encoder | 把输入序列编码成向量表示 |
| Decoder | 从编码表示逐步生成输出序列 |
